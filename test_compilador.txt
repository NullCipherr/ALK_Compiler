// Teste de todos os tipos de dados
int numero = 42;
float pi = 3.14159;
char letra = 'A';
string mensagem = "Ola, mundo!";

// Teste de vetores
int numeros[10];
float valores[5];
char letras[3];
string nomes[2];

// Teste de estrutura de decisão
if (numero > 0) {
    numero = numero + 1;
} else {
    numero = 0;
}

// Teste de estrutura de repetição
while (numero < 10) {
    numero = numero + 1;
}

// Teste de sub-rotina
void funcaoTeste(int a, float b) {
    int resultado;
    resultado = a * 2;
    return resultado;
}

// Teste de operadores
int a = 10;
int b = 20;
int soma = a + b;
int subtracao = a - b;
int multiplicacao = a * b;
int divisao = b / a;

// Teste de comparações
if (a == b) {
    return 1;
}
if (a != b) {
    return 0;
}
if (a < b) {
    return -1;
}
if (a > b) {
    return 2;
}

// Teste de identificadores complexos
int contador123;
float temperatura_maxima;
string nomeCompleto;

// Teste de valores diversos
int numeroGrande = 123456;
float numeroDecimal = 0.123;
char simbolo = '#';
string textoLongo = "Este eh um texto mais longo para teste";

// Teste de erros léxicos
// @variavel = 123;    // Deve gerar erro
// 123abc = 456;       // Deve gerar erro
// "string não fechada; // Deve gerar erro