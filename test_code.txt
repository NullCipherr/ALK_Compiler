// Teste de declarações básicas com diferentes tipos
create int contador as 0;
create float temperatura as 37.5;
create char status as 'A';
create string mensagem as "Sistema iniciado";

// Teste de vetores
create int numeros[5] as array;
create float notas[10] as array;
create char letras[26] as array;
create string nomes[3] as array;

// Teste de declaração duplicada (deve gerar erro)
create int contador as 42;

// Teste de função com retorno
function int calcular_soma(int a, int b) {
    create int resultado as a plus b;
    give back resultado;
}

// Teste de função void
function void mostrar_mensagem(string msg) {
    // Simulando print (built-in)
    give back;
}

// Teste de estrutura de decisão com tipos diferentes
check (temperatura is_greater_than 37.0) then {
    mensagem = "Temperatura elevada";
    status = 'E';
} otherwise {
    mensagem = "Temperatura normal";
    status = 'N';
}

// Teste de estrutura de repetição com incremento
repeat while (contador is_less_than 5) {
    contador = contador plus 1;
    temperatura = temperatura plus 0.5;
}

// Teste de operações aritméticas com tipos diferentes
create float media as 0.0;
media = (temperatura plus 36.5) divided_by 2.0;

// Teste de operações com strings (deve gerar erro)
mensagem = mensagem plus " - Finalizado";  // Erro: operação inválida para strings

// Teste de chamada de função
create int soma_teste as calcular_soma(10, 20);

// Teste de chamada de função com número errado de argumentos (deve gerar erro)
create int erro_soma as calcular_soma(5);

// Teste de uso de variável não declarada (deve gerar erro)
create int teste as valor_inexistente plus 10;

// Teste de atribuição com tipos incompatíveis (deve gerar aviso)
contador = temperatura;  // Aviso: possível perda de precisão

// Teste de operações relacionais
check (contador equals 5) then {
    status = 'C';
}

check (temperatura not_equals 38.0) then {
    status = 'D';
}

check (contador is_less_than 10) then {
    contador += 1;  // Teste do operador +=
}

// Teste de expressões complexas
create float resultado_complexo as (temperatura minus 32.0) times 5.0 divided_by 9.0;

// Teste de chamada de função built-in
print(mensagem);
scan(temperatura);

// Teste de acesso a vetor
numeros[0] = 42;
notas[contador] = temperatura;
